<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lyro Admin | Create NBT Test</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        bg: {
                            primary: '#0a0e17',
                            secondary: '#141824',
                            tertiary: '#1d2230',
                        },
                        accent: {
                            teal: '#1ab69d',
                            orange: '#ff6b35',
                        }
                    },
                    fontFamily: {
                        sans: ['Inter', 'sans-serif'],
                    }
                }
            }
        }
    </script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
</head>

<body class="bg-bg-primary text-gray-200 font-sans min-h-screen flex">

    <%- include('sidebar', { page: 'nbt' }) %>

        <!-- Main Content -->
        <div class="flex-1 p-8 overflow-y-auto">
            <div class="max-w-2xl mx-auto">
                <div class="flex items-center gap-4 mb-8">
                    <a href="/admin/nbt"
                        class="w-10 h-10 rounded-full bg-white/5 flex items-center justify-center text-gray-400 hover:bg-white/10 transition-all">
                        <i class="fas fa-arrow-left"></i>
                    </a>
                    <h1 class="text-3xl font-bold text-white tracking-tight">Create NBT Test</h1>
                </div>

                <div class="bg-bg-secondary/50 backdrop-blur-sm border border-white/5 rounded-2xl p-8 shadow-xl">
                    <form action="/admin/nbt/create" method="POST" class="space-y-6">

                        <div class="space-y-2">
                            <label class="text-sm font-medium text-gray-400">Test Title</label>
                            <input type="text" name="title" required placeholder="e.g. NBT Prep Test 1"
                                class="w-full bg-bg-tertiary border border-white/10 rounded-xl px-4 py-3 text-white focus:outline-none focus:border-accent-teal/50 focus:ring-1 focus:ring-accent-teal/50 transition-all">
                        </div>

                        <div class="space-y-2">
                            <label class="text-sm font-medium text-gray-400">Description (Optional)</label>
                            <textarea name="description" rows="3"
                                class="w-full bg-bg-tertiary border border-white/10 rounded-xl px-4 py-3 text-white focus:outline-none focus:border-accent-teal/50 transition-all"></textarea>
                        </div>

                        <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                            <div class="space-y-2">
                                <label class="text-sm font-medium text-gray-400">Available From</label>
                                <input type="datetime-local" name="availableFrom" required
                                    class="w-full bg-bg-tertiary border border-white/10 rounded-xl px-4 py-3 text-white focus:outline-none focus:border-accent-teal/50 transition-all calendar-icon-white">
                            </div>
                            <div class="space-y-2">
                                <label class="text-sm font-medium text-gray-400">Available Until</label>
                                <input type="datetime-local" name="availableUntil" required
                                    class="w-full bg-bg-tertiary border border-white/10 rounded-xl px-4 py-3 text-white focus:outline-none focus:border-accent-teal/50 transition-all">
                            </div>
                        </div>

                        <div class="space-y-2">
                            <label class="text-sm font-medium text-gray-400">Test Duration (Minutes)</label>
                            <input type="number" name="durationMinutes" value="60" min="1" required
                                class="w-full bg-bg-tertiary border border-white/10 rounded-xl px-4 py-3 text-white focus:outline-none focus:border-accent-teal/50 transition-all">
                        </div>

                        <button type="submit"
                            class="w-full bg-accent-teal hover:bg-teal-600 text-white font-bold py-4 rounded-xl shadow-lg shadow-accent-teal/20 mt-4 transition-all">
                            Create & Add Questions
                        </button>

                    </form>
                </div>
            </div>
        </div>

        <style>
            input[type="datetime-local"]::-webkit-calendar-picker-indicator {
                filter: invert(1);
                cursor: pointer;
            }
        </style>
</body>

</html>
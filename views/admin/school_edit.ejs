<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lyro Admin | Edit School</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        bg: {
                            primary: '#0a0e17',
                            secondary: '#141824',
                            tertiary: '#1d2230',
                        },
                        accent: {
                            teal: '#1ab69d',
                            orange: '#ff6b35',
                            purple: '#8b5cf6',
                        }
                    },
                    fontFamily: {
                        sans: ['Inter', 'sans-serif'],
                    }
                }
            }
        }
    </script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
</head>

<body class="bg-bg-primary text-gray-200 font-sans min-h-screen flex">

    <%- include('sidebar', { page: 'schools' }) %>

        <!-- Main Content -->
        <div class="flex-1 p-8 overflow-y-auto">
            <div class="max-w-2xl mx-auto">
                <div class="flex items-center gap-4 mb-8">
                    <a href="/admin/schools"
                        class="w-10 h-10 rounded-full bg-white/5 flex items-center justify-center text-gray-400 hover:bg-white/10 transition-all">
                        <i class="fas fa-arrow-left"></i>
                    </a>
                    <h1 class="text-3xl font-bold text-white tracking-tight">Edit School</h1>
                </div>

                <div
                    class="bg-bg-secondary/50 backdrop-blur-sm border border-white/5 rounded-2xl p-6 shadow-xl relative animate-fade-in">

                    <form action="/admin/schools/update/<%= school._id %>" method="POST" class="space-y-6">
                        <div class="space-y-2">
                            <label class="text-sm font-medium text-gray-400">School Name</label>
                            <input type="text" name="name" value="<%= school.name %>" required
                                class="w-full bg-bg-tertiary border border-white/10 rounded-xl px-4 py-3 text-white focus:outline-none focus:border-accent-teal/50">
                        </div>

                        <div class="space-y-2">
                            <label class="text-sm font-medium text-gray-400">Grades</label>
                            <div class="flex gap-4 p-4 bg-bg-tertiary border border-white/10 rounded-xl">
                                <label class="flex items-center gap-2 cursor-pointer">
                                    <input type="checkbox" name="grades" value="12" <%=school.grades.includes(12)
                                        ? 'checked' : '' %> class="accent-accent-teal w-5 h-5">
                                    <span class="text-white">Grade 12</span>
                                </label>
                                <label class="flex items-center gap-2 cursor-pointer">
                                    <input type="checkbox" name="grades" value="11" <%=school.grades.includes(11)
                                        ? 'checked' : '' %> class="accent-accent-teal w-5 h-5">
                                    <span class="text-white">Grade 11</span>
                                </label>
                                <label class="flex items-center gap-2 cursor-pointer">
                                    <input type="checkbox" name="grades" value="10" <%=school.grades.includes(10)
                                        ? 'checked' : '' %> class="accent-accent-teal w-5 h-5">
                                    <span class="text-white">Grade 10</span>
                                </label>
                            </div>
                        </div>

                        <button type="submit"
                            class="w-full bg-accent-teal hover:bg-teal-600 text-white font-bold py-3 px-6 rounded-xl transition-all shadow-lg shadow-accent-teal/20">
                            Update School
                        </button>
                    </form>

                </div>
            </div>
        </div>
</body>

</html>
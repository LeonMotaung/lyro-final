<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lyro Admin | NBT Prep</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        bg: {
                            primary: '#0a0e17',
                            secondary: '#141824',
                            tertiary: '#1d2230',
                        },
                        accent: {
                            teal: '#1ab69d',
                            orange: '#ff6b35',
                        }
                    },
                    fontFamily: {
                        sans: ['Inter', 'sans-serif'],
                    }
                }
            }
        }
    </script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
</head>

<body class="bg-bg-primary text-gray-200 font-sans min-h-screen flex">

    <%- include('sidebar', { page: 'nbt' }) %>

        <!-- Main Content -->
        <div class="flex-1 p-8 overflow-y-auto">
            <div class="max-w-4xl mx-auto">
                <div class="flex justify-between items-center mb-8">
                    <h1 class="text-3xl font-bold text-white tracking-tight">NBT Prep Tests</h1>
                    <a href="/admin/nbt/create"
                        class="bg-accent-teal hover:bg-teal-600 text-white px-6 py-2 rounded-xl font-bold transition-all shadow-lg shadow-accent-teal/20">
                        <i class="fas fa-plus mr-2"></i> Create New Test
                    </a>
                </div>

                <div
                    class="bg-bg-secondary/50 backdrop-blur-sm border border-white/5 rounded-2xl overflow-hidden shadow-xl">
                    <table class="w-full text-left border-collapse">
                        <thead>
                            <tr class="bg-white/5 border-b border-white/5">
                                <th class="p-4 text-xs font-bold text-gray-400 uppercase tracking-wider">Test Title</th>
                                <th class="p-4 text-xs font-bold text-gray-400 uppercase tracking-wider">Availability
                                    Window</th>
                                <th class="p-4 text-xs font-bold text-gray-400 uppercase tracking-wider">Questions</th>
                                <th class="p-4 text-xs font-bold text-gray-400 uppercase tracking-wider text-right">
                                    Actions</th>
                            </tr>
                        </thead>
                        <tbody class="divide-y divide-white/5">
                            <% if (tests && tests.length> 0) { %>
                                <% tests.forEach(test=> { %>
                                    <tr class="hover:bg-white/5 transition-colors">
                                        <td class="p-4">
                                            <div class="text-white font-bold">
                                                <%= test.title %>
                                            </div>
                                            <div class="text-xs text-gray-500">
                                                <%= test.durationMinutes %> mins allowed
                                            </div>
                                        </td>
                                        <td class="p-4 text-sm text-gray-400">
                                            <div>From: <span class="text-white">
                                                    <%= new Date(test.availableFrom).toLocaleString() %>
                                                </span></div>
                                            <div>Until: <span class="text-white">
                                                    <%= new Date(test.availableUntil).toLocaleString() %>
                                                </span></div>
                                        </td>
                                        <td class="p-4">
                                            <span class="bg-white/10 px-2 py-1 rounded text-xs text-white">
                                                <%= test.questions ? test.questions.length : 0 %> Qs
                                            </span>
                                        </td>
                                        <td class="p-4 text-right flex justify-end gap-2">
                                            <a href="/admin/nbt/edit/<%= test._id %>"
                                                class="bg-white/5 hover:bg-accent-teal hover:text-white text-gray-400 p-2 rounded-lg transition-all text-xs"
                                                title="Edit Questions">
                                                <i class="fas fa-edit"></i>
                                            </a>
                                            <form action="/admin/nbt/delete/<%= test._id %>" method="POST"
                                                onsubmit="return confirm('Delete this test?');" style="display:inline;">
                                                <button type="submit"
                                                    class="bg-white/5 hover:bg-red-500 hover:text-white text-gray-400 p-2 rounded-lg transition-all text-xs"
                                                    title="Delete">
                                                    <i class="fas fa-trash"></i>
                                                </button>
                                            </form>
                                        </td>
                                    </tr>
                                    <% }) %>
                                        <% } else { %>
                                            <tr>
                                                <td colspan="4" class="p-8 text-center text-gray-500">
                                                    No NBT tests created yet.
                                                </td>
                                            </tr>
                                            <% } %>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
</body>

</html>
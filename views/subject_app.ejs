<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lyro Tutor | <%= subject.name %>
    </title>
    <link rel="stylesheet" href="/css/styles.css">
    <link rel="stylesheet" href="/css/learn.css">
    <link rel="stylesheet" href="/css/responsive.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
</head>

<body>
    <div class="learn-screen">
        <div class="header">
            <a href="/learn<%= subject.grade %>" class="back-link"><i class="fas fa-arrow-left"></i></a>
            <div class="brand">
                <div class="brand-name">
                    <%= subject.name %>
                </div>
            </div>
            <!-- Toggle removed -->
        </div>

        <main class="content-wrapper">
            <div class="hero" style="min-height: 120px;">
                <div class="hero-title">
                    <%= subject.name %>
                </div>
                <div class="hero-sub">Grade <%= subject.grade %>
                </div>
            </div>

            <div class="topic-list" style="padding: 20px;">
                <% if (topics && topics.length> 0) { %>
                    <div class="grid-topics"
                        style="display: grid; grid-template-columns: repeat(auto-fill, minmax(300px, 1fr)); gap: 15px;">
                        <% topics.forEach(topic=> { %>
                            <a href="/practice?subject=<%= encodeURIComponent(subject.name) %>&grade=<%= subject.grade %>&topic=<%= encodeURIComponent(topic.name) %>"
                                class="topic-card"
                                style="background: rgba(255,255,255,0.05); padding: 20px; border-radius: 15px; text-decoration: none; color: white; border: 1px solid rgba(255,255,255,0.1); display: flex; align-items: center; justify-content: space-between; transition: all 0.3s;">
                                <span>
                                    <%= topic.name %>
                                </span>
                                <i class="fas fa-chevron-right" style="color: var(--accent-teal);"></i>
                            </a>
                            <% }) %>
                    </div>
                    <% } else { %>
                        <div class="empty-state" style="text-align: center; color: #888; margin-top: 50px;">
                            <i class="fas fa-folder-open"
                                style="font-size: 48px; margin-bottom: 20px; opacity: 0.5;"></i>
                            <p>No topics added yet.</p>
                        </div>
                        <% } %>
            </div>
        </main>
    </div>
</body>

</html>